// einmal mit allem bitte
#include <bits/stdc++.h>
// wer will schon immer std:: schreiben
using namespace std;
// schreibfaul aber wissen was drin ist
typedef int32_t i32;
typedef int64_t i64;
typedef uint32_t u32;
typedef uint64_t u64;
// besser ist das
#define float fliesskommazahlensindboese
#define double einfachnichtbenutzen
// und los

int main() {
    i32 noOfTestCases;
    cin >> noOfTestCases;

    while (noOfTestCases) {
        i32 noOfHints;
        i32 noOfSwitches;

        cin >> noOfHints >> noOfSwitches;

        if (noOfHints < noOfSwitches-1) {
            cout << "missing hints" << endl;
        }










        noOfTestCases--;
    }
}